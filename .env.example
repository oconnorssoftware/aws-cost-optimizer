# AWS Credentials
# Option 1: Use AWS CLI profile
AWS_PROFILE=default

# Option 2: Use access keys directly (not recommended for production)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Default region
AWS_DEFAULT_REGION=us-east-1